package org.taskmanager;

public class Capacity {
    // MAXIMUM_CAPACITY is set in build.gradle for tests and maybe set in any building engine
    static int MAXIMUM_CAPACITY = Integer.parseInt(System.getProperty("MAXIMUM_CAPACITY"));

}
